{
  "name": "kokoro-enogu-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute kokoro-enogu --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute kokoro-enogu --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260820.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
